# SkyShow Collision Alert — Assignment 1

Student: `<Shakhriyar Nurlanov>`, group `<SE-2512>`, barcode `<251567>`

## How to run

From the project folder (the one that contains `src/`):

```
javac src/*.java -d out
java -cp out Main
```

IntelliJ IDEA: open the project folder, right-click `src` → Mark Directory as → Sources Root, run `Main`.

## Java version

`<the version you used, e.g. 21.0.4>`

## What I did

- Part A — Brute Force: `<done>`
- Part B — Divide & Conquer: `<done>`, strip is `<sorted on every level>`
- Part C — Safety limit: `<done>`

## Assumptions

`<Distances are compared using squared distances, so no square root is needed.>` <br>
`<The input array is not modified; Part B works with a cloned and sorted copy.>` <br>
`<The strip is sorted by y-coordinate at every recursive level.>` 
